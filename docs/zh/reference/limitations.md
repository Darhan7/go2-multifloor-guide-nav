# 工程边界与下一步

一个可继续使用的工程记录，不只要展示已经跑通的部分，也要把下一次接手时最值得做的事情说清楚。下面这些不是“项目作废的缺口”，而是现场条件恢复后可以直接继续的任务清单。

## 回到机器人旁边后优先补齐

- 重新建立并调试三楼 `amcl_foxy.yaml`，保存为公开 example 和现场参数两个版本；
- 在一楼现场记录经过验证的大厅、电梯和讲解点坐标；
- 把 floor switch、目标楼层初始位姿和电梯状态机接入统一任务脚本；
- 将语音 intent adapter 与语义 YAML / building graph 正式连通；
- 用最终划定的地图工作边界完成一轮实机安全验证。

## 归档中没有保留的历史材料

- 当时使用的精确 `localization/config/amcl_foxy.yaml`；
- 已编译 `graph_pid_ws` 的源码来源；
- 每次建图的完整原始传感器 rosbag。

这些内容不会阻止读者理解系统或复现 planner-only 流程。它们只意味着换到新环境时，需要像正常机器人项目一样重新校准与验证。

## 关于历史文件名

历史目录 `floor_3_recovered` 实际表示第二次更认真完成的三楼采集，并不是从损坏文件或 rosbag 中恢复得到。公开文档统一称它为 **Floor 3 second pass**。

## 可公开材料

地图、地点名称和语义坐标已获得老师同意公开；Go2 实物照片会在后续补入首页或系统架构页。
